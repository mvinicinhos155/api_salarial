version: "3.9"

services:

  postgres:
    image: postgres:16
    container_name: postgres_db
    environment:
      POSTGRES_USER: pirlo
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: financias
    ports:
      - "5432:5432"
